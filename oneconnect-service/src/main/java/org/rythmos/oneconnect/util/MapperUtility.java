package org.rythmos.oneconnect.util;

public class MapperUtility {
	/*
	 * 
	 * @Autowired ModelMapper modelMapper;
	 * 
	 * @Bean public ModelMapper modelMapper () { return new ModelMapper(); }
	 * 
	 * S sourceObj; T targetObj;
	 * 
	 * public MapperUtility (S sourceObj, T targetObj) { this.sourceObj = sourceObj;
	 * this.targetObj = targetObj; } public MapperUtility () { }
	 * 
	 * @SuppressWarnings("unchecked") public T getTargetObject(S sourceObj) { return
	 * (T) modelMapper.map (sourceObj, (Class<T>) getClass()); }
	 */}
